.codex/config.toml
.DS_Store
.codex/version.json
/.codex/tmp
/.codex/.tmp
.codex/.personality_migration
.codex/auth.json
/.codex/cache
.codex/*.sqlite
.codex/*.sqlite-*
.codex/history.jsonl
.codex/installation_id
.codex/models_cache.json
/.codex/sessions
/.codex/shell_snapshots
/.codex/plugins/cache
/.codex/.sandbox_migration
/.codex/thread-writer-locks
.codex/session_index.jsonl

# 各 Agent 在本仓的本地 skill 安装目录 —— 不能进版本库:
# skills.sh 的 PRIORITY_PREFIXES 会把 .claude/skills/、.agents/skills/、.codex/skills/
# 下的 SKILL.md 一并算作本仓提供的 skill(.codex/skills/.system/ 是 Codex 自带的系统
# skill,更不是我们的)。本仓对外提供的 skill 只有 skills/ 一处。
.claude/skills/
.agents/skills/
.codex/skills/
/skills/swarm-workspace
skills/swarm/evals/evals.json
.env.cf

# engine 二进制不进库也不进 npm 包(见 scripts/publish-vendor-r2.mjs):
# 由 vendor 脚本抓到本地,发版时传 R2,装机端按 VERSION.json 的 url 取。
/vendor/impeccable/engine/
